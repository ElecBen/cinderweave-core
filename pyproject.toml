[project]
name = "cinderweave-core"
version = "0.2.0"
description = "Distancia de edicion entre dos cadenas"
requires-python = ">=3.9"
license = { text = "MIT" }
keywords = ["levenshtein", "hamming", "distancia", "texto"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
