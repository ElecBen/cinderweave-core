[project]
name = "cinderweave-core"
version = "0.1.0"
description = "Distancia de edicion entre dos cadenas"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
